package nitrox.org.ktboard.application.service

import io.kotest.core.spec.style.ExpectSpec
import io.kotest.core.spec.style.FunSpec
import org.junit.jupiter.api.Test

import org.junit.jupiter.api.Assertions.*

internal class BoardServiceExpectSpecTest : ExpectSpec() {

    init {
        expect("Archive Boards with no columns") {

        }

        expect("Archive Boards with columns and finalized tasks") {

        }

        expect("Archive Boards with columns and active tasks") {

        }
    }
}